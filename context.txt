================================================================================
JOB SCHEDULER — PROJECT CONTEXT
Last updated: 2026-08-22
================================================================================

This file tracks everything built in this project so far. Append new entries
at the bottom whenever changes are made.

--------------------------------------------------------------------------------
1. PROJECT OVERVIEW
--------------------------------------------------------------------------------

A full-stack distributed job queue manager. Users log in via OAuth (Google /
GitHub), create Projects, define Queues inside those projects, and enqueue Jobs.
A Spring Boot worker process polls queues, executes jobs, handles retries, and
manages a dead-letter queue for exhausted jobs.

Tech stack:
  - Backend  : Java 21, Spring Boot 3.3.2, Spring Security, Spring Data JPA
  - Database : Supabase-hosted PostgreSQL (transaction pooler, ap-southeast-1)
  - Auth     : Supabase Auth (OAuth — Google + GitHub), JWT (ES256 / ECDSA P-256)
  - Frontend : React 18, TypeScript, Vite, React Router v6, Supabase JS SDK, Axios
  - Build    : Maven (backend), npm / Vite (frontend)

--------------------------------------------------------------------------------
2. REPOSITORY STRUCTURE
--------------------------------------------------------------------------------

Job-Scheduler/
├── .env                          # Root-level env (not used directly by app)
├── context.txt                   # This file
├── database/
│   └── schema.sql                # Full PostgreSQL schema — run in Supabase SQL Editor
├── backend/
│   ├── pom.xml
│   └── src/main/
│       ├── java/com/jobscheduler/
│       │   ├── JobSchedulerApplication.java
│       │   ├── controller/       # REST controllers
│       │   ├── dto/              # Request / Response DTOs
│       │   ├── entity/           # JPA entities
│       │   ├── repository/       # Spring Data JPA repositories
│       │   ├── security/         # JWT filter, JwtService, SecurityConfig, UserContext
│       │   ├── service/          # Business logic
│       │   └── worker/           # Background scheduler threads
│       └── resources/
│           └── application.properties
└── frontend/
    ├── package.json
    ├── .env                      # VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
    ├── .env.example
    └── src/
        ├── App.tsx               # Route tree
        ├── main.tsx
        ├── api/                  # Axios API clients
        ├── components/           # Layout, ProtectedRoute
        ├── hooks/                # useSupabaseAuth, usePolling, useSessionReady
        ├── lib/                  # supabaseClient.ts, axios.ts, slug.ts
        ├── pages/                # All page components
        ├── styles/               # Global CSS
        └── types/                # Shared TypeScript types (index.ts)

--------------------------------------------------------------------------------
3. DATABASE SCHEMA  (database/schema.sql)
--------------------------------------------------------------------------------

Tables:
  users                 — id (UUID = Supabase auth.users.id), email, name, avatar_url
  organizations         — id, name, slug
  organization_members  — user_id + org_id composite PK, role
  projects              — id, user_id (FK→users), org_id (FK→organizations), name, api_key_prefix
  queues                — id, project_id (FK→projects), name, concurrency_limit (default 5),
                          is_paused (default false), default_retry_config (JSONB)
  workers               — id, name (UNIQUE), last_heartbeat_at, status (active|draining|offline)
  jobs                  — id, queue_id (FK→queues), status (pending|scheduled|claimed|running|
                          completed|failed|dead), priority (SMALLINT), scheduled_at,
                          next_retry_at, locked_at, locked_by, attempt_count, max_attempts,
                          retry_config (JSONB), payload (JSONB), last_error
  job_attempts          — id (BIGSERIAL), job_id, attempt_number, worker_name, started_at,
                          finished_at, error_stack
  dead_letter_entries   — id, job_id, original_payload (JSONB), error, died_at

Key indexes:
  idx_jobs_poller       — (queue_id, priority DESC, scheduled_at) WHERE status IN (pending, scheduled)
  idx_jobs_reaper       — (status, locked_at) WHERE status = claimed
  idx_queues_project_name — UNIQUE (project_id, name)
  idx_jobs_user_lookup  — (queue_id)
  idx_job_attempts_job_id — (job_id, started_at DESC)

Triggers:
  set_updated_at()      — auto-updates updated_at column on users, organizations, projects, jobs

Note: VARCHAR + CHECK constraints used instead of PG enum types for full
Hibernate @Enumerated(STRING) compatibility.

--------------------------------------------------------------------------------
4. BACKEND — ENTITIES  (backend/src/main/java/com/jobscheduler/entity/)
--------------------------------------------------------------------------------

Job.java
  - Fields: id (UUID), queue (ManyToOne→Queue), status (JobStatus enum), priority,
    scheduledAt, nextRetryAt, lockedAt, lockedBy, attemptCount, maxAttempts,
    retryConfig (JSONB Map), payload (JSONB Map), lastError, createdAt, updatedAt
  - OneToMany → JobAttempt (cascade ALL)
  - Enum JobStatus: pending, scheduled, claimed, running, completed, failed, dead
  - Uses hypersistence-utils JsonBinaryType for JSONB columns

Queue.java
  - Fields: id, project (ManyToOne→Project), name, concurrencyLimit, isPaused,
    defaultRetryConfig (JSONB Map)

Project.java
  - Fields: id, user (ManyToOne→User), org (ManyToOne→Organization), name, apiKeyPrefix

User.java
  - Fields: id (UUID = Supabase sub claim), email, name, avatarUrl

Organization.java / OrganizationMember.java
  - Standard org + membership model (role-based)

Worker.java
  - Fields: id, name (UNIQUE), lastHeartbeatAt, status (WorkerStatus enum: active, draining, offline)

JobAttempt.java
  - Fields: id (Long), job (ManyToOne), attemptNumber, workerName, startedAt, finishedAt, errorStack

DeadLetterEntry.java
  - Fields: id, job (ManyToOne), originalPayload (JSONB), error, diedAt

--------------------------------------------------------------------------------
5. BACKEND — REPOSITORIES  (backend/src/main/java/com/jobscheduler/repository/)
--------------------------------------------------------------------------------

JobRepository         — custom queries: claimJobs (SELECT FOR UPDATE SKIP LOCKED),
                        markRunning, findByQueueIdAndStatus, findByUserId,
                        countByStatusForQueues, promoteScheduledJobs, findStaleClaimed
QueueRepository       — findByProjectId, findByIdAndProjectId, findAllActive, findByUserId,
                        existsByProjectIdAndName, upsertHeartbeat
ProjectRepository     — findByUserIdOrderByCreatedAtDesc, findByIdAndUserId
WorkerRepository      — findStaleWorkers, upsertHeartbeat
JobAttemptRepository  — findByJobIdOrderByStartedAtDesc
DeadLetterRepository  — standard CRUD
OrganizationRepository / UserRepository — standard CRUD

--------------------------------------------------------------------------------
6. BACKEND — SERVICES  (backend/src/main/java/com/jobscheduler/service/)
--------------------------------------------------------------------------------

JobService.java
  - enqueue()        : creates Job with status=pending or scheduled depending on scheduledAt
  - listByQueue()    : paginated query, optional status filter
  - listByUser()     : paginated, cross-queue view for a user
  - get()            : fetch single job
  - getAttempts()    : fetch all JobAttempts for a job
  - claimJobs()      : SELECT FOR UPDATE SKIP LOCKED — atomically claims N jobs
  - markRunning()    : transitions claimed → running
  - completeJob()    : records attempt, sets status=completed
  - failJob()        : records attempt, applies retry logic or moves to dead
  - cancelJob()      : sets status=dead (not allowed on running/claimed)
  - requeueDeadJob() : resets dead/failed job back to pending with attempt_count=0
  - resetStuckJob()  : called by Reaper, resets stuck claimed jobs to pending
  - calculateNextRetry() : supports exponential (default, with jitter), linear, fixed strategies
  - effectiveMaxAttempts(): cascading priority — job retryConfig > queue defaultRetryConfig > job.maxAttempts
  - moveToDead()     : creates DeadLetterEntry when retries exhausted

ProjectService.java
  - listForUser(), getForUser(), create(), rename(), delete()
  - generateKeyPrefix() : generates "js_" + 8-char random hex prefix

QueueService.java
  - listForProject(), getForProject(), create(), update(), pause(), resume(), delete()
  - verifyOwnership() : checks project belongs to requesting user

--------------------------------------------------------------------------------
7. BACKEND — CONTROLLERS  (backend/src/main/java/com/jobscheduler/controller/)
--------------------------------------------------------------------------------

JobController         — /api/queues/{queueId}/jobs
  POST   /api/queues/{queueId}/jobs            enqueue a job
  GET    /api/queues/{queueId}/jobs            list jobs (status filter + pagination)
  GET    /api/jobs                             list all jobs for current user (paginated)
  GET    /api/jobs/{jobId}                     get single job
  GET    /api/jobs/{jobId}/attempts            get all attempts for a job
  POST   /api/jobs/{jobId}/cancel              cancel a pending/scheduled job
  POST   /api/jobs/{jobId}/requeue             re-queue a dead/failed job

ProjectController     — /api/projects
  GET    /api/projects                         list user's projects
  GET    /api/projects/{id}                    get single project
  POST   /api/projects                         create project
  PUT    /api/projects/{id}                    rename project
  DELETE /api/projects/{id}                    delete project

QueueController       — /api/projects/{projectId}/queues
  GET    /api/projects/{projectId}/queues                          list queues
  GET    /api/projects/{projectId}/queues/{queueId}               get single queue
  POST   /api/projects/{projectId}/queues                         create queue
  PATCH  /api/projects/{projectId}/queues/{queueId}               update (concurrency/pause/retry)
  POST   /api/projects/{projectId}/queues/{queueId}/pause         pause queue
  POST   /api/projects/{projectId}/queues/{queueId}/resume        resume queue
  DELETE /api/projects/{projectId}/queues/{queueId}               delete queue

DashboardController   — GET /api/dashboard
  Returns: job counts by status, total jobs/queues/projects, all workers with heartbeat

GlobalExceptionHandler
  - EntityNotFoundException   → 404
  - IllegalArgumentException  → 400
  - IllegalStateException     → 409
  - MethodArgumentNotValidException → 422 with field error map
  - Exception (catch-all)     → 500

--------------------------------------------------------------------------------
8. BACKEND — DTOs  (backend/src/main/java/com/jobscheduler/dto/)
--------------------------------------------------------------------------------

JobRequest      — payload, priority, scheduledAt, maxAttempts, retryConfig
JobResponse     — all Job fields as flat record, static from(Job) factory
ProjectRequest  — name (validated)
ProjectResponse — id, name, apiKeyPrefix, createdAt, queueCount
QueueRequest    — name, concurrencyLimit, defaultRetryConfig
QueueResponse   — id, projectId, name, concurrencyLimit, isPaused, defaultRetryConfig
DashboardResponse — jobCountsByStatus (Map), totalJobs, totalQueues, totalProjects, workers list
PageResponse<T> — content, totalElements, totalPages, page, size

--------------------------------------------------------------------------------
9. BACKEND — SECURITY  (backend/src/main/java/com/jobscheduler/security/)
--------------------------------------------------------------------------------

SecurityConfig.java
  - Stateless session (no HTTP session)
  - CSRF disabled
  - CORS: configured via cors.allowed-origins property
    Allowed origins: http://localhost:5173, http://localhost:3000
    Allowed methods: GET, POST, PUT, PATCH, DELETE, OPTIONS
    Allowed headers: Authorization, Content-Type, Accept
    Exposed headers: X-Total-Count
  - Public endpoints: /actuator/health, /actuator/info, OPTIONS /**
  - All other requests require authentication
  - Returns 401 (not 302) for unauthenticated requests

JwtService.java
  - Fetches Supabase JWKS at startup from:
    https://zqoyyspgigcugjpnmwus.supabase.co/auth/v1/.well-known/jwks.json
  - Validates ES256 (ECDSA P-256) signed JWTs, no secret needed
  - Caches PublicKey objects by kid; supports fallback to single key
  - Extracts: userId (sub claim), email, name (from user_metadata), avatarUrl

JwtAuthenticationFilter.java
  - OncePerRequestFilter — runs on every request
  - Extracts Bearer token from Authorization header
  - On valid token: auto-provisions User record on first login, syncs name/avatar
  - Populates UserContext (ThreadLocal) and Spring SecurityContext
  - Always clears ThreadLocal + SecurityContext in finally block

UserContext.java
  - ThreadLocal<User> holder — set by filter, consumed by controllers/services

--------------------------------------------------------------------------------
10. BACKEND — BACKGROUND WORKERS  (backend/src/main/java/com/jobscheduler/worker/)
--------------------------------------------------------------------------------

CronScheduler.java
  - promoteScheduledJobs()  : every 10 s — flips scheduled→pending for jobs whose
                              scheduled_at has passed
  - heartbeat()             : every 15 s — upserts worker heartbeat into workers table
  - markOffline()           : @PreDestroy — marks worker offline on graceful shutdown

JobPoller.java
  - poll()                  : every 2 s — iterates all active (non-paused) queues,
                              respects per-queue Semaphore (concurrencyLimit permits)
  - executeAsync()          : @Async — marks job running, calls processJob(), then
                              completeJob() or failJob()
  - processJob()            : stub — simulates 100–500 ms work (replace with real dispatch)
  - Per-queue ConcurrentHashMap<UUID, Semaphore> prevents over-saturation

Reaper.java
  - reap()                  : every 60 s
    - reaperSweepJobs()     : finds claimed jobs locked > 5 min, resets to pending
    - reaperSweepWorkers()  : marks workers with no heartbeat in 60 s as offline

--------------------------------------------------------------------------------
11. BACKEND — CONFIGURATION  (backend/src/main/resources/application.properties)
--------------------------------------------------------------------------------

server.port=8081
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
  ?user=postgres.zqoyyspgigcugjpnmwus&password=Pratik@216029
spring.jpa.hibernate.ddl-auto=validate   (schema managed manually via schema.sql)
supabase.jwks.url=https://zqoyyspgigcugjpnmwus.supabase.co/auth/v1/.well-known/jwks.json
spring.task.execution.pool.core-size=4
spring.task.execution.pool.max-size=16
spring.task.execution.pool.queue-capacity=100
worker.name=spring-boot-instance-1
cors.allowed-origins=http://localhost:5173,http://localhost:3000
management.endpoints.web.exposure.include=health,info,metrics

Backend Maven dependencies:
  spring-boot-starter-web, data-jpa, security, validation, actuator
  postgresql driver
  jjwt-api/impl/jackson 0.12.6  (JWT validation)
  hypersistence-utils-hibernate-63 3.7.6  (JSONB support)
  jackson-databind
  lombok

--------------------------------------------------------------------------------
12. FRONTEND — PAGES  (frontend/src/pages/)
--------------------------------------------------------------------------------

LoginPage.tsx
  - Public page; Google + GitHub OAuth buttons via useSupabaseAuth
  - Redirects to / if session already exists

AuthCallbackPage.tsx
  - OAuth redirect landing page
  - Waits for Supabase to parse session from URL, then navigates to / or /login

DashboardPage.tsx
  - Polls every 5 s via usePolling
  - Shows: 4 stat cards (total jobs, projects, queues, active workers)
  - Job status breakdown grid + stacked progress bar
  - Workers table with name, status badge, last heartbeat timestamp

ProjectsPage.tsx
  - Polls every 15 s
  - Grid of project cards with queue count, slug preview, created date
  - Inline "New Project" form with live slug preview
  - Delete with confirmation

QueuesPage.tsx
  - URL param: :projectName (slug)
  - Resolves slug → project ID via projectsApi.list() + findBySlug()
  - Polls every 10 s
  - Lists queues with concurrency, retry config, active/paused badge
  - Inline "New Queue" form (name + concurrency)
  - Pause/Resume toggle, Delete

JobsPage.tsx
  - URL params: :projectName (slug), :queueName (slug) — both optional
  - If both present: queue-specific view; if absent: global /jobs view
  - Polls every 4 s
  - Status filter buttons (All + each status)
  - Paginated table (20 per page) with expandable rows showing payload, lastError, lockedBy
  - Inline "Enqueue Job" form with JSON textarea + validation
  - Cancel (pending/scheduled) and Requeue (dead/failed) actions

--------------------------------------------------------------------------------
13. FRONTEND — COMPONENTS  (frontend/src/components/)
--------------------------------------------------------------------------------

Layout.tsx
  - Sidebar with logo, nav links (Dashboard, Projects, All Jobs), user info, sign-out
  - NavLink active state with indigo highlight
  - Renders <Outlet /> for nested routes

ProtectedRoute.tsx
  - Wraps protected routes; spinner while loading, Navigate to /login if no session

--------------------------------------------------------------------------------
14. FRONTEND — HOOKS  (frontend/src/hooks/)
--------------------------------------------------------------------------------

useSupabaseAuth.ts
  - Central auth hook; wraps supabase.auth.getSession + onAuthStateChange
  - Returns: session, user (AuthUser shape), loading, signInWithGoogle,
    signInWithGitHub, signOut

usePolling.ts
  - Runs fn() immediately then on every intervalMs
  - Accepts enabled flag (pauses when false, e.g. session not ready)
  - Ref-based to always call latest closure

useSessionReady.ts
  - Returns boolean true once a valid Supabase session is confirmed
  - Used to gate API calls — prevents 401s on page load before token is available

--------------------------------------------------------------------------------
15. FRONTEND — API CLIENTS  (frontend/src/api/)
--------------------------------------------------------------------------------

All clients use a shared Axios instance (lib/axios.ts) that:
  - Baseurl = /api (proxied by Vite dev server to http://localhost:8081)
  - Automatically attaches Authorization: Bearer <token> from Supabase session

dashboard.ts  — dashboardApi.get()
projects.ts   — projectsApi: list, get, create, rename, delete
queues.ts     — queuesApi: list, get, create, update, pause, resume, delete
jobs.ts       — jobsApi: listByQueue, listMine, get, attempts, enqueue, cancel, requeue

--------------------------------------------------------------------------------
16. FRONTEND — TYPES  (frontend/src/types/index.ts)
--------------------------------------------------------------------------------

AuthUser, Project, Queue, RetryConfig, Job, JobAttempt, WorkerSummary,
DashboardData, Page<T>, JobStatus (union type), WorkerStatus (union type)

--------------------------------------------------------------------------------
17. FRONTEND — ROUTING  (frontend/src/App.tsx)
--------------------------------------------------------------------------------

/login                                        — public (LoginPage)
/auth/callback                                — public (AuthCallbackPage)
/ (index)                                     — protected (DashboardPage)
/projects                                     — protected (ProjectsPage)
/projects/:projectName                        — protected (QueuesPage)
/projects/:projectName/queues/:queueName/jobs — protected (JobsPage — queue view)
/jobs                                         — protected (JobsPage — global view)
* → redirect to /

All protected routes wrapped in ProtectedRoute > Layout (sidebar shell).

--------------------------------------------------------------------------------
18. FRONTEND — SLUG UTILITY  (frontend/src/lib/slug.ts)
--------------------------------------------------------------------------------

toSlug(name)     — converts project/queue name to URL-safe slug (lowercase, hyphens)
findBySlug(list, slug) — finds a project or queue by matching its name slug

--------------------------------------------------------------------------------
19. SUPABASE PROJECT
--------------------------------------------------------------------------------

Project ref : zqoyyspgigcugjpnmwus
Region      : ap-southeast-1
JWKS URL    : https://zqoyyspgigcugjpnmwus.supabase.co/auth/v1/.well-known/jwks.json
Auth        : Google OAuth + GitHub OAuth enabled
Frontend env:
  VITE_SUPABASE_URL      = https://zqoyyspgigcugjpnmwus.supabase.co
  VITE_SUPABASE_ANON_KEY = sb_publishable_fHTSxTvTvoH4mbIHXS-fYA_hRzYkEGn

--------------------------------------------------------------------------------
20. HOW TO RUN
--------------------------------------------------------------------------------

Backend:
  cd backend
  mvn spring-boot:run
  → Starts on http://localhost:8081

Frontend:
  cd frontend
  npm install
  npm run dev
  → Starts on http://localhost:5173 (proxies /api → :8081)

Database:
  Run database/schema.sql once in Supabase SQL Editor.

--------------------------------------------------------------------------------
21. KNOWN DESIGN DECISIONS
--------------------------------------------------------------------------------

- No Docker / deployment config exists yet.
- No tests written yet (test directory is empty).
- JobPoller.processJob() is a stub (Thread.sleep simulation). Real job dispatch
  should route by payload "type" to registered JobHandler beans.
- Organization model exists in schema/entities but is not wired into the API yet
  (no OrganizationController).
- api_key_prefix on Project is generated but no API-key-based auth path exists yet.
- Slug-based routing (projectName/queueName in URLs) resolved by fetching all
  items and doing client-side slug match — not passed as a query param to backend.
- CORS is applied to all paths (not just /api/**) so error responses (401, 500)
  also carry correct CORS headers.

================================================================================
CHANGE LOG
================================================================================

2026-08-22  Initial context.txt created.
            Captured full project state: DB schema, all entities, repositories,
            services, controllers, DTOs, security layer, 3 background workers,
            all frontend pages/components/hooks/API clients, routing, and types.

================================================================================

2026-08-22  Fixed OAuth redirect-back-to-login race condition.

            Root causes:
            1. AuthCallbackPage called getSession() immediately after the OAuth
               redirect. Supabase hadn't finished parsing the URL hash token yet,
               so getSession() returned null and the page navigated to /login.
            2. useSupabaseAuth called both getSession() AND onAuthStateChange.
               Between getSession() resolving (null) and onAuthStateChange firing
               (with the real session), loading was false + session was null —
               causing ProtectedRoute to redirect to /login.

            Fixes applied:
            - AuthCallbackPage (frontend/src/pages/AuthCallbackPage.tsx):
              Now checks getSession() first; if null, subscribes to
              onAuthStateChange and waits for the SIGNED_IN event before
              navigating. Added a 5 s safety timeout fallback.
            - useSupabaseAuth (frontend/src/hooks/useSupabaseAuth.ts):
              Removed the getSession() call entirely. Now relies solely on
              onAuthStateChange, which fires INITIAL_SESSION with the current
              session (or null) and is the authoritative signal to set
              loading=false. Subscribing first ensures no event is missed.


2026-08-22  Fixed worker_status PostgreSQL enum type mismatch error.

            Error: "column status is of type worker_status but expression is of
            type character varying" thrown by CronScheduler.heartbeat() on every
            scheduled tick.

            Root cause: The live Supabase database had a real PostgreSQL enum
            type `worker_status` on workers.status (and possibly `job_status` on
            jobs.status). The schema.sql file correctly defines these as
            VARCHAR(20) + CHECK constraints for Hibernate @Enumerated(STRING)
            compatibility, but the live DB was out of sync.

            Fix: Run the following in Supabase SQL Editor to align the live DB
            with schema.sql:

              ALTER TABLE workers
                ALTER COLUMN status TYPE VARCHAR(20)
                USING status::text;
              ALTER TABLE workers
                ADD CONSTRAINT workers_status_check
                CHECK (status IN ('active', 'draining', 'offline'));

            Also verify jobs.status with:
              SELECT column_name, data_type, udt_name
              FROM information_schema.columns
              WHERE table_name IN ('workers', 'jobs') AND column_name = 'status';
            If udt_name shows job_status (not varchar), apply same ALTER TABLE
            to jobs table.

            No Java code changes needed — the entity and repository were already
            correct. upsertHeartbeat in WorkerRepository already used CAST to
            handle the native query path.


2026-08-22  Added 5 named executor queues with real backend logic and frontend payload templates.

            Backend — new package: backend/src/main/java/com/jobscheduler/executor/

            JobExecutor.java (interface)
              - queueName() : String  — the queue name this executor handles
              - execute(Job) : void   — throws on failure to trigger retry

            DemoTaskExecutor.java
              - Queue name : "DemoTaskExecutor"
              - Payload    : { duration: int (seconds), shouldFail: boolean }
              - Logic      : sleeps in 500 ms chunks for `duration` seconds,
                             then throws if shouldFail=true

            EmailJobExecutor.java
              - Queue name : "EmailJobExecutor"
              - Payload    : { to, subject, body }
              - Logic      : validates required fields, simulates SMTP round-trip (300 ms),
                             logs delivery. Replace Thread.sleep with real JavaMailSender/SES.

            NotificationJobExecutor.java
              - Queue name : "NotificationJobExecutor"
              - Payload    : { userId, message, channel (push|sms|in-app, default: in-app) }
              - Logic      : routes to simulatePush/simulateSms/simulateInApp.
                             Replace stubs with FCM/APNs/WebSocket calls.

            PdfJobExecutor.java
              - Queue name : "PdfJobExecutor"
              - Payload    : { fileName, fileUrl }
              - Logic      : downloads PDF via Java HttpClient, validates magic bytes (%PDF),
                             enforces 20 MB size limit. Replace stub with PDFBox/iText.

            CalculateExecutor.java
              - Queue name : "CalculateExecutor"
              - Payload    : { operation (SUM|AVERAGE|MIN|MAX|MULTIPLY), values: number[] }
              - Logic      : validates inputs, runs Java streams calculation, logs result.

            JobPoller.java (rewritten)
              - Constructor now accepts List<JobExecutor> from Spring DI
              - Builds executorRegistry map: queueName.toLowerCase() → executor bean
              - dispatch(job, queueName) looks up executor by queue name (case-insensitive)
              - Falls back to fallbackStub (200–600 ms sleep) for unregistered queues
              - Logs registered executors at startup

            Frontend — JobsPage.tsx updated

            QUEUE_PAYLOAD_TEMPLATES map (keyed by lower-case queue name):
              demotaskexecutor, emailjobexecutor, notificationjobexecutor,
              pdfjobexecutor, calculateexecutor

            EXECUTOR_DESCRIPTIONS map: one-line hint shown below the page title
            when viewing a known executor queue.

            Enqueue form changes:
              - showEnqueue useEffect: pre-fills payloadText with template on open
              - "↺ Reset to template" button restores the sample payload
              - Blue left-border hint bar: "Sample payload pre-filled — edit as needed"
              - textarea border turns red on JSON parse error
              - "executor queue" badge shown in queue metadata row for known queues


2026-08-22  Fixed persistent redirect-to-login after successful OAuth sign-in (second attempt).

            Root causes found:
            1. React.StrictMode double-invokes useEffect in development. The
               AuthCallbackPage subscription was torn down and recreated after
               the SIGNED_IN event already fired — causing the timeout fallback
               to redirect to /login every time.
            2. The cleanup return inside AuthCallbackPage was inside a .then()
               callback, so React could never call it. Subscription and timeout
               were never properly cleaned up.
            3. useSupabaseAuth fired onAuthStateChange INITIAL_SESSION with null
               before getSession() resolved, setting loading=false + session=null,
               causing ProtectedRoute to redirect before the real session arrived.

            Fixes applied:
            - main.tsx: Removed React.StrictMode entirely (it breaks Supabase
              OAuth token parsing in development via double-invoke).
            - AuthCallbackPage.tsx: Full rewrite. Subscription and timeout stored
              in let variables (not inside .then), cleanup returned directly from
              useEffect. navigatedRef prevents double-navigation on StrictMode
              double-invoke. Subscribes first, then calls getSession() as fast
              path. 10 s timeout safety net.
            - useSupabaseAuth.ts: Restored getSession() as the authoritative
              initial-load mechanism (sets loading=false). onAuthStateChange now
              skips INITIAL_SESSION (already handled). mounted flag prevents
              setState after unmount.


2026-08-22  Fixed two backend errors: prepared statement cache conflict and worker_status enum.

            Error 1: "prepared statement S_x already exists"
            Cause: Supabase transaction pooler (port 6543) hands out a fresh
            PostgreSQL backend connection per transaction. Hibernate's server-side
            prepared statement cache creates named statements (S_1, S_2, …) that
            don't survive connection recycling. On the next transaction, Hibernate
            tries to reuse S_x on a fresh connection that doesn't have it → error.
            Fix: Added to JDBC URL:
              prepareThreshold=0            — disable server-side PS creation
              preparedStatementCacheQueries=0 — disable driver-side PS cache
            Also added hikari.keepalive-time=60000 to keep idle connections alive.
            File: backend/src/main/resources/application.properties

            Error 2: "column status is of type worker_status but expression is of
            type character varying" — still occurring because the Supabase DB still
            has a real PG enum type on workers.status.
            Fix (Java side — works regardless of whether DB is enum or VARCHAR):
            - Worker.java: Replaced @Enumerated(EnumType.STRING) with
              @Convert(converter = WorkerStatusConverter.class). The converter
              writes a PGobject of type "worker_status" so PostgreSQL receives
              a properly typed value in INSERT/UPDATE.
            - WorkerRepository.java: Changed findByStatus() to a native query
              with CAST(:status AS worker_status). Changed findStaleWorkers() to
              a native query to avoid JPQL enum binding issues with the converter.
            Files: Worker.java, WorkerRepository.java

            Still recommended: run the ALTER TABLE fix in Supabase SQL Editor
            to convert workers.status from enum to VARCHAR (see entry 2026-08-22
            "Fixed worker_status PostgreSQL enum type mismatch error") so the
            schema matches schema.sql. The Java fix above is a resilient fallback
            that works either way.


2026-08-22  Added EnqueueJobModal — proper form-based enqueue UI with queue selector and field editors.

            New file: frontend/src/components/EnqueueJobModal.tsx

            Features:
            - Project dropdown (loads all user projects)
            - Queue dropdown (loads queues for selected project, auto-selects first)
            - For known executor queues: renders labeled input fields per queue type
              (text, email, number, textarea, select, boolean toggle)
            - For unknown queues: falls back to raw JSON textarea editor
            - Priority (0–100) and Max Attempts (1–20) options section
            - Submit calls jobsApi.enqueue, closes on success
            - Click outside overlay to close
            - Can be opened with lockedQueue prop (skips selectors, used from queue/jobs pages)

            Executor field definitions (EXECUTOR_DEFS map):
              emailjobexecutor        → to (email), subject (text), body (textarea)
              notificationjobexecutor → userId (number), message (textarea), channel (select)
              demotaskexecutor        → duration (number), shouldFail (boolean toggle)
              pdfjobexecutor          → fileName (text), fileUrl (text)
              calculateexecutor       → operation (select), values (comma-separated text → number[])

            Updated: frontend/src/pages/QueuesPage.tsx
              - Added "+ Enqueue" button on each queue row
              - Opens EnqueueJobModal with lockedQueue set to that queue

            Updated: frontend/src/pages/JobsPage.tsx
              - Removed inline enqueue form
              - "+ Enqueue Job" button now opens EnqueueJobModal with lockedQueue=current queue
              - Removed QUEUE_PAYLOAD_TEMPLATES and EXECUTOR_DESCRIPTIONS (moved to modal)


2026-08-22  Enhanced EnqueueJobModal with schema panel and fuzzy queue name matching.

            Changes to frontend/src/components/EnqueueJobModal.tsx:

            Schema panel:
            - "{ } Schema" button in the modal header (visible for executor queues)
            - Slides open below the header showing a table: Field | Type | Required | Notes
            - Each row shows the field key (monospace), schema type, required flag, and
              options/hints
            - Followed by a "EXAMPLE PAYLOAD" pre block showing the full JSON template

            Field labels now show a type pill (e.g. "string", "number", "boolean",
            "number[]", '"SUM" | "AVERAGE" | ...') inline next to the label text.

            Queue name matching now handles all real-world naming variants:
            - Exact lower-case match
            - Normalised match (strips spaces, hyphens, underscores)
            - Covers: "email", "EmailJobExecutor", "email-job", "Demo-Task",
              "demo_task", "Pdf-Extract", "pdfextract", "Calculate", etc.
            - getExecutorDef(queueName) tries lower → normalised → null (raw JSON fallback)


2026-08-22  Fixed LazyInitializationException in JobPoller + wired real email sending.

            Bug: Jobs failed with "could not initialize proxy [Queue#...] — no Session".
            Root cause: executeAsync() is @Async — it runs in a separate thread with no
            JPA session. Calling job.getQueue().getName() inside that thread triggered a
            lazy load on the Queue proxy, which failed because the session was closed.

            Fix (JobPoller.java):
            - poll() now resolves queue.getName() while still in the main thread (session open)
            - Passes resolvedQueueName as a plain String into executeAsync()
            - executeAsync signature: executeAsync(Job job, String queueName, Semaphore sem)
            - No more lazy proxy access inside the async thread

            Also added real email sending:
            - Added spring-boot-starter-mail to pom.xml
            - Rewrote EmailJobExecutor to inject JavaMailSender and call mailSender.send()
            - Added spring.mail.* config block to application.properties (Gmail/SMTP)
            - User must fill in spring.mail.username and spring.mail.password (App Password)
              before email jobs will actually deliver. See:
              https://myaccount.google.com/apppasswords

            To configure email:
            1. Go to Google Account → Security → App Passwords
            2. Generate an app password for "Mail"
            3. Set spring.mail.username=your-email@gmail.com
               Set spring.mail.password=the-16-char-app-password
            4. Restart backend


2026-08-22  Implemented Redis sliding-window rate limiting using Upstash.

            Dependencies added (pom.xml):
              spring-boot-starter-data-redis  (includes Lettuce TLS client)

            New files:
              backend/src/main/java/com/jobscheduler/config/RedisConfig.java
                - Parses upstash.redis.url to extract host/port
                - Creates LettuceConnectionFactory with TLS (useSsl())
                - Registers StringRedisTemplate bean
                - Registers slidingWindowScript bean (DefaultRedisScript<Long>)
                  Lua script: ZREMRANGEBYSCORE (prune old) → ZCARD (count) →
                  if count < limit: ZADD + PEXPIRE → return 1 else return 0

              backend/src/main/java/com/jobscheduler/ratelimit/RateLimiterService.java
                - isAllowed(userId, bucket): executes Lua script, returns boolean
                - remaining(userId, bucket): ZCOUNT for header population
                - Fails open on Redis error (logs warning, allows request)
                - Buckets: "default" (100 req/60s) and "enqueue" (20 req/60s)
                - Key format: rl:<userId>:<bucket>

              backend/src/main/java/com/jobscheduler/ratelimit/RateLimitFilter.java
                - OncePerRequestFilter, registered BEFORE JwtAuthenticationFilter
                - Skips: OPTIONS, /actuator/**, non-/api/** paths
                - Key: userId from JWT (if valid) OR client IP (fallback)
                - Bucket: POST /api/queues/*/jobs → "enqueue", else "default"
                - On allowed: sets X-RateLimit-Limit/Remaining/Reset headers
                - On blocked: HTTP 429 + Retry-After header + JSON body:
                  { status, error, message, retryAfterSeconds }

            Updated files:
              SecurityConfig.java: injects RateLimitFilter, registers it before
                JwtAuthenticationFilter via addFilterBefore()
              application.properties: added Upstash Redis + rate limit config

            application.properties keys added:
              upstash.redis.url        — rediss://... URL from Upstash console
              upstash.redis.password   — Redis password from Upstash console
              spring.data.redis.url    — delegates to upstash.redis.url
              spring.data.redis.password
              spring.data.redis.ssl.enabled=true
              ratelimit.enabled=true
              ratelimit.default.limit=100
              ratelimit.default.window-seconds=60
              ratelimit.enqueue.limit=20
              ratelimit.enqueue.window-seconds=60

            To configure Upstash:
              1. Go to console.upstash.com → create a Redis database
              2. Copy the "Redis URL" (starts with rediss://)
              3. Copy the password (shown separately)
              4. Set upstash.redis.url and upstash.redis.password in application.properties
              5. Restart backend


2026-08-22  Fixed executor queue name mismatch — executors were not routing.

            Bug: All jobs ran the "fallback stub" (200-600ms sleep) instead of their
            real executor. Log showed: "No executor registered for queue 'email'".
            Root cause: queueName() in each executor returned the class name
            (e.g. "EmailJobExecutor") but the actual queues were named "email",
            "Calculate", "Demo-Task", "Notification", "Pdf-Extract".
            Fix: Updated queueName() in each executor to match actual queue names:
              EmailJobExecutor     → "email"
              CalculateExecutor    → "Calculate"
              DemoTaskExecutor     → "Demo-Task"
              NotificationJobExecutor → "Notification"
              PdfJobExecutor       → "Pdf-Extract"


2026-08-22  Implemented result storage + display for Calculate queue.

            Backend:
            - Job.java: Added result JSONB field (@Type JsonBinaryType)
            - JobService.java: Added storeResult(UUID jobId, Map result)
            - CalculateExecutor.java: Calls jobService.storeResult() after computing,
              stores { operation, values, result } map
            - JobResponse.java: Added result field + j.getResult() in from()
            Database: ALTER TABLE jobs ADD COLUMN IF NOT EXISTS result JSONB;

            Frontend:
            - types/index.ts: Added result: Record<string,unknown>|null to Job interface
            - JobsPage.tsx: Added JobResult component that renders per executor type:
                Calculate    → green card: Operation, Values, big result number
                Demo-Task    → indigo card: Status, Duration, Message in quotes
                Notification → teal card: Status, Recipient, Channel, Message, timestamp
                PDF          → orange card: File, Pages, Words, Size, Summary pre-block
                Generic      → raw JSON fallback


2026-08-22  Overhauled all 3 remaining executors + PDF upload + notification inbox.

            ── EXECUTOR REWRITES ─────────────────────────────────────────────

            DemoTaskExecutor.java
              - Payload: { "message": "Hello from Demo Task" }
              - Logs: "Demo Task Started" → sleeps 3 s → logs message → "Demo Task Completed"
              - Stores result: { status, message, startedAt, finishedAt, durationMs }
              - Injected: JobService

            NotificationJobExecutor.java
              - Payload changed: userId (number) → userEmail (string email)
              - Looks up User by email via UserRepository.findByEmail()
              - Saves Notification entity to notifications table (user inbox)
              - Stores result: { status, userEmail, channel, message, deliveredAt }
              - Injected: UserRepository, NotificationRepository, JobService

            PdfJobExecutor.java
              - Uses Apache PDFBox 3.0.3 (added to pom.xml)
              - Downloads PDF from fileUrl, validates %PDF magic bytes
              - Extracts text with Loader.loadPDF + PDFTextStripper
              - Stores result: { fileName, sizeBytes, pageCount, wordCount, summary (500 chars) }
              - Injected: JobService

            ── NEW BACKEND FILES ─────────────────────────────────────────────

            entity/Notification.java
              - Fields: id, user (ManyToOne→User), title, message, channel, isRead, createdAt
              - Table: notifications

            repository/NotificationRepository.java
              - findByUserIdOrderByCreatedAtDesc
              - countByUserIdAndIsReadFalse
              - markAllReadForUser  (JPQL UPDATE)
              - markOneRead(id, userId) (JPQL UPDATE)

            controller/NotificationController.java
              - GET  /api/notifications              → list all for current user
              - GET  /api/notifications/unread-count → { count: N }
              - POST /api/notifications/mark-all-read
              - POST /api/notifications/{id}/mark-read
              - Inner record: NotificationDto

            controller/UploadController.java
              - POST /api/upload/pdf          → save PDF to temp dir, return { fileName, fileUrl }
              - GET  /api/upload/files/{name} → serve saved file (public, no auth)
              - upload.dir defaults to java.io.tmpdir

            ── MODIFIED BACKEND FILES ────────────────────────────────────────

            SecurityConfig.java
              - Added: .requestMatchers("/api/upload/files/**").permitAll()
              - Fixes HTTP 401 when PdfJobExecutor downloads the uploaded file

            pom.xml
              - Added: org.apache.pdfbox:pdfbox:3.0.3

            ── DATABASE CHANGES ──────────────────────────────────────────────

            notifications table (already run in Supabase):
              CREATE TABLE notifications (
                id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
                title TEXT NOT NULL DEFAULT 'Notification',
                message TEXT NOT NULL,
                channel VARCHAR(20) NOT NULL DEFAULT 'in-app',
                is_read BOOLEAN NOT NULL DEFAULT FALSE,
                created_at TIMESTAMPTZ DEFAULT NOW()
              );
              Indexes: idx_notifications_user_id, idx_notifications_unread

            jobs table:
              ALTER TABLE jobs ADD COLUMN IF NOT EXISTS result JSONB;

            ── NEW FRONTEND FILES ────────────────────────────────────────────

            frontend/src/api/notifications.ts
              - notificationsApi: list(), unreadCount(), markAllRead(), markOneRead(id)
              - NotificationItem interface

            frontend/src/components/InboxPanel.tsx
              - Slide-in panel from right (380px wide) with backdrop
              - Lists all notifications, unread highlighted in indigo
              - Click notification → mark as read
              - "Mark all read" button
              - Channel emoji: 📩 in-app, 🔔 push, 💬 sms
              - Props: onClose, onRead

            ── MODIFIED FRONTEND FILES ───────────────────────────────────────

            frontend/src/components/Layout.tsx
              - Added Inbox button in sidebar (below "All Jobs")
              - Unread badge (accent color, count) shown when unread > 0
              - Polls unread count every 10 s
              - Opens InboxPanel on click

            frontend/src/components/EnqueueJobModal.tsx
              - Notification: userId input → userEmail email input
              - Demo-Task: replaced duration/shouldFail → single "message" textarea
              - Pdf-Extract: replaced text fileUrl → dashed file-picker that uploads
                via POST /api/upload/pdf and auto-fills fileName + fileUrl
              - New pdf-upload field type with file input ref + upload progress state

            frontend/src/pages/JobsPage.tsx
              - Notification result card: shows userEmail as Recipient, not userId
              - "Stored in recipient's inbox" note added to notification result card

            ── KNOWN ISSUES ─────────────────────────────────────────────────

            - PDF upload stores files in system temp dir (not persistent — use S3 in prod)
            - NotificationJobExecutor throws if recipient email not found in users table
              (user must have logged in at least once)
            - Notification push/sms channels are still simulated (Thread.sleep)

